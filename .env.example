# Copie para .env; este exemplo é apenas para execução local.
DJANGO_SECRET_KEY=django-insecure-apenas-desenvolvimento-local
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
# Sem DATABASE_URL, o sistema utiliza SQLite (db.sqlite3).
